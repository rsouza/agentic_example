[project]
name = "agentic-example-hf-space"
version = "0.1.0"
description = "HF Spaces app for DH pipeline with a local Hugging Face model"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "gradio>=4.44.0",
  "transformers>=4.44.0",
  "torch>=2.3.0",
  "python-dotenv>=1.0.1",
]
